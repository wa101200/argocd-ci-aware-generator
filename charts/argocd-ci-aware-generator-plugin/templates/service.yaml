apiVersion: v1
kind: Service
metadata:
  name: {{ include "argocd-ci-aware-generator-plugin.fullname" . }}
  labels:
    {{- include "argocd-ci-aware-generator-plugin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "argocd-ci-aware-generator-plugin.selectorLabels" . | nindent 4 }}
