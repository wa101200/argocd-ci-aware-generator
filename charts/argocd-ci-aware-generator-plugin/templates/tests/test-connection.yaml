apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "argocd-ci-aware-generator-plugin.fullname" . }}-test-connection"
  labels:
    {{- include "argocd-ci-aware-generator-plugin.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "argocd-ci-aware-generator-plugin.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
