apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-validate-ci-checks-generators-plugin-latest
  namespace: argocd
data:
  token: "$plugin.ci.token" # Alternatively $<some_K8S_secret>:plugin.myplugin.token
  baseUrl: "http://speech-to-text-latest.privatelink.francecentral.azmk8s.io"
  requestTimeout: "60"
